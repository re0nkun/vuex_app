version: '3'
services:
  web:
    build: .
    tty: true
    command: npm run dev
    # command: yarn serve
    volumes:
      - .:/app
    ports:
      - "8080:8080"

# dcrw vue init webpack .
# dcrw vue create .
